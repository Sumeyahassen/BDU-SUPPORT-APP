# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration
# Use localhost because MongoDB will run inside the same container
MONGO_URI=mongodb://localhost:27017/bdu_exam_app

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRE=24h

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# CORS Configuration
ALLOWED_ORIGINS=*
